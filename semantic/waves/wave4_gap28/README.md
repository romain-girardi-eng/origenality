# gap_2026_08

One line per record, classified against the controlled vocabulary of
themes, works of Origen, approaches and relevance. This page is written
by the publication pass from `tags.jsonl` alone: every figure below is
recovered by counting the file next to it.

| | |
|---|---|
| Records | 24 (24 distinct) |
| Classified on | 2026-08-16 |
| Prompt version | tag-notice-v2.1 |
| Vocabulary version | approaches=1.0.0;relevance=1.0.0;themes=1.1.0;works=1.0.0 |
| Flagged for review | 0 |
| Also in another wave file here | 24, under their last class |

Relevance: **core 18**, **partial 4**, marginal 2, none 0. A published
figure counts `core` and `partial` and nothing else, which makes **22**
records here. A `marginal` record is returned by a search and listed under
a count without entering it; a `none` record is what the harvest brought in
and the classification pushed out. `docs/methodology.md` states the rule and
what it costs.

The classification is machine-assisted; `confidence` and `needs_review` are
carried on every line, and its agreement with hand tagging is not published
(see the limits in the root `README.md`).
